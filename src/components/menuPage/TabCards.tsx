interface ITabCards {
  smallHeader: string;
  mainHeader: string;
}

const TabCards = ({ smallHeader, mainHeader }: ITabCards) => {
  return <div className="my-6"></div>;
};

export default TabCards;
